#include "cliente.h"
#include <stdio.h>

// Por enquanto, deixamos vazio para o programa rodar.
// Amanhã você coloca a lógica de cadastro aqui.

void InicializarClientes() {
    // Stub (função oca apenas para compilar)
}